/**
 * Created by saikumaradepu on 3/6/15.
 */
import java.util.Vector;
public class MapHW {

    private Vector keys;
    private Vector values;

    public MapHW()
    {
        keys = new Vector();
        values = new Vector();
    }
public void put()
{
    //EFFECTS: add the key-value pair to 'this' if key is not present
    //          else changes the value of the key in 'this'
    //MODIFIES: this

}
    public void remove()
    {
        //EFFECTS: removes the key-value pair if it exists in the Map
        //
        //MODIFIES: this_post-> this -
    }

    public void contains()
    {
        //EFFECTS: returns 'true' if the passed key is present in the map.

    }

    public  void size()
    {
        //EFFECTS: returns the size of the map, i.e number of key-value pairs
    }
   /* put – Modifies the value of a key-value pair. If the key is not in the map, it adds the key-value pair to the map
    remove – Removes the key-value pair from the map if it exists
    contains – Returns true if a key is in the map
    size – Returns the size of the map*/
}
